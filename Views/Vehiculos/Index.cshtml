@model IEnumerable<SisParkTD.Models.Vehiculos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Patente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DescripcionDeVehiculo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TiposDeVehiculo.NombreTipoDeVehiculo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Patente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DescripcionDeVehiculo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TiposDeVehiculo.NombreTipoDeVehiculo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDVehiculo }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDVehiculo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDVehiculo })
        </td>
    </tr>
}

</table>
